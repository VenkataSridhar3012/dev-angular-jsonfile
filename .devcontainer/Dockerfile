# Use the official Node.js 16 base image
FROM node:16

# Install the Angular CLI globally
RUN npm install -g @angular/cli

# Keep the rest of the dev container configuration
COPY .devcontainer/ /workspace/.devcontainer/
